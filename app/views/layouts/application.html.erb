<!DOCTYPE html>
<html>
  <head>
    <title><%= meta_title %></title>
    <meta name="description" content="width=device-width, initial-scale=1, shrink-to-fit=no <%= meta_description %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%# Mapbox settings %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></script>

    <%# Japanese style icons %>
    <link rel="stylesheet" href="https://yannklein.github.io/japan-pictograms/japan-pictograms.css">

  </head>

  <body>
    <% unless devise_controller? || params[:controller] == "stamp_cards" || params[:controller] == "shop_participants" %>
      <%= render "shared/navbar" %>
      <%= render "shared/flashes" %>
    <% end %>
    <div class="content">
      <%= yield %>
    </div>
  </body>
</html>
