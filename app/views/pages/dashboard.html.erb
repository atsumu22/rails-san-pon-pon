<%# Checks if current user is chairman or not %>
  <% if user_signed_in? %>
    <%= render current_user.status == "chairperson" ? "chairman_dash" : "user_dash" %>
  <% else %>
    <%= render "user_dash" %>
  <% end %>
